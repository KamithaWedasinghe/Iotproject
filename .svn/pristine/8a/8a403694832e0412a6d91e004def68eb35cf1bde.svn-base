import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { EmployeeRegistationComponent } from './employee-registation.component';

describe('EmployeeRegistationComponent', () => {
  let component: EmployeeRegistationComponent;
  let fixture: ComponentFixture<EmployeeRegistationComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ EmployeeRegistationComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(EmployeeRegistationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
