import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DeductionAlowancesComponent } from './deduction-alowances.component';

describe('DeductionAlowancesComponent', () => {
  let component: DeductionAlowancesComponent;
  let fixture: ComponentFixture<DeductionAlowancesComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DeductionAlowancesComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DeductionAlowancesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
